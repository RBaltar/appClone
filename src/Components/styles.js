import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  
    barraInf: {
      backgroundColor: '#000',
      width: 500,
      height: 60,
      bottom: 0,
      position: 'absolute',
      flexDirection: 'row',
    }

})